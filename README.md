## in the root:

```bash
npm install
```

## in the client:

```bash
cd client
npm install
```

## .env in the root:

```bash
MONGO = ' '
JWT_SECRET = ' '
```

## .env in the client:

```bash
VITE_FIREBASE_API_KEY = " "
```
